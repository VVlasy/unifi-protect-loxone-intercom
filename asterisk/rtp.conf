;==============================================================================
; rtp.conf  -  media port range. Loxone RTP + the internal ARI externalMedia
; channel both draw from here. Keep these open on the host (host networking).
;==============================================================================
[general]
rtpstart = 10000
rtpend = 10200
