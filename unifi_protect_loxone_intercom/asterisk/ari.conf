;==============================================================================
; ari.conf  -  Asterisk REST Interface (the bridge connects here)
; The password is injected at container start from the ARI_PASS env var.
;==============================================================================
[general]
enabled = yes
pretty = yes
allowed_origins = *

[doorbellari]
type = user
read_only = no
password = __ARI_PASS__
