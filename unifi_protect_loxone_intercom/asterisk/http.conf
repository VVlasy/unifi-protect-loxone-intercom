;==============================================================================
; http.conf  -  required for ARI. Bound to localhost: only the in-container
; bridge talks to it, never the network.
;==============================================================================
[general]
enabled = yes
bindaddr = 127.0.0.1
bindport = 8088
