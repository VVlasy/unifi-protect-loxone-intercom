[general]
[logfiles]
console => notice,warning,error
; Security events (failed auth, bad/anonymous requests, ACL rejects) to their own
; file so fail2ban can watch it and ban SIP scanners. /var/log/asterisk is bind-
; mounted to the LXC (see docker-compose.yml) where fail2ban reads this file.
security => security
